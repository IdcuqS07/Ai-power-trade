# Legacy Scripts

This folder contains archived scripts that are no longer part of the active workflow.

## Archived

- `migrate-to-polygon.sh`
- `super-migrate-polygon.sh`

These scripts were kept only for historical reference because they:

- are tied to an old migration flow
- still reference legacy repo/account assumptions
- no longer reflect the current project layout

## Active Replacement

Use the root-level active scripts instead, especially:

- `quick-deploy.sh`
- `deploy-*.sh`
- `run.sh`
