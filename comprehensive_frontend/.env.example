# NEXT_PUBLIC_CLAUDE_API_KEY=
# Backend base URL for frontend serverless routes and direct client calls
NEXT_PUBLIC_API_URL=http://localhost:8000
# Optional alias for deployments that already use this name
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
